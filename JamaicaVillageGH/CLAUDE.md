# CLAUDE.md

`JamaicaVillageGH/` is a distinct sub-project (an investment/education hub, not a festival chapter) with its own `assets/css/styles.css` and `assets/js/scripts.js`, and sections for `about`, `contact`, `invest`, `lessons`, `media`. Its nav is meant to be canonical/identical across all its pages.
